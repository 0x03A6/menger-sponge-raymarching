max_iter_rough 5
max_iter_relative 6.0
max_iter_relative_cutoff 30.0
max_raymarching_iter 350
cutoff_dist_far 2000000.0
cutoff_dist_near_rough 0.0001
cutoff_dist_near_relative 0.00005
epsilon 0.00001
// base_color_formula abs(normal) * 0.5 + 0.5	// simple RGB shading.
base_color_formula transpose(mat3(view_inv)) * normal * 0.5 + 0.5	// Color varies while rotating the camera.
// base_color_formula vec3(0.9, 0.9, 1.0)	// Single color shading.
// base_color_formula abs(rd_world)	// Shade with ray direction only.
// base_color_formula mix(vec3(0.1, 0.2, 0.8), vec3(1.0, 0.8, 0.3), pow(1.0 - max(dot(normal, -rd_world), 0.0), 2.0))	// Something cool written by GPT. Recommend you give it a try.
